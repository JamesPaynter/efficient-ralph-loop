# Codex configuration for workers (flat keys per Codex SDK)

# Model used by the worker thread
model = "gpt-5.2-codex"
model_reasoning_effort = "xhigh"

# Approval policy: "never" for unattended runs
approval_policy = "never"

# Sandbox mode: Docker provides isolation for the workspace
sandbox_mode = "danger-full-access"
