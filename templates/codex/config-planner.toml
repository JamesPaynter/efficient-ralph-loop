# Codex configuration for the planner phase.
#
# Goal: allow codebase reading but prevent repo modifications.

model = "gpt-5.2-codex"
model_reasoning_effort = "xhigh"
approval_policy = "never"
sandbox_mode = "read-only"
