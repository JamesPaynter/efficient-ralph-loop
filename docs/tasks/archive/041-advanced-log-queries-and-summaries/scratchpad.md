# 2026-01-11

## Notes

# 2026-01-15

## Notes
- Added indexed `logs timeline`, `logs failures`, multi-source `--follow`, and LLM-backed task summaries (config gated).
- Recorded doctor/test summaries, codex turn info, and next-action heuristics in `logs summarize`.
- Tests: `npm test`
- Build: `npm run build`
